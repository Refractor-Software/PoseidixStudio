#once

#if __OSX__
    Print('$MsgPrefix$ Building for the macOS platform.')
    Print('$MsgPrefix$ Using Apple Clang Compiler.')

    .bBuildMac      = true
    #define __USE_CLANG__

    ; todo: file extensions

    .BinOut + '\macOS'
    .ObjOut + '\macOS'

    ; very temporary, Apple Clang will need its own solution later
    #include "../LLVM_Collect.bff"
#endif ; __OSX__